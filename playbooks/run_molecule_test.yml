---
- name: Run Molecule tests for molecule_test_role
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Run molecule test
      ansible.builtin.command:
        cmd: molecule test -s default
        chdir: "{{ playbook_dir }}/../roles/molecule_test_role"
      environment:
        ANSIBLE_FORCE_COLOR: "true"
      register: molecule_result

    - name: Display molecule output
      ansible.builtin.debug:
        var: molecule_result.stdout_lines
